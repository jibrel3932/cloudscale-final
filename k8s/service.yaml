apiVersion: v1
kind: Service
metadata:
  name: cloudscale-service
  labels:
    app: cloudscale
spec:
  type: LoadBalancer
  selector:
    app: cloudscale
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
